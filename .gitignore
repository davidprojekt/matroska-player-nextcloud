/node_modules
# Vite build output (emitted into the app root by @nextcloud/vite-config).
/js
/css
/dist
/assets
# Bundled ffmpeg.wasm core, built from @matroska-js/player's recipe (npm run build:ffmpeg) and
# copied in by npm run setup:ffmpeg; a build artifact — never committed.
/ffmpeg
# Staged deployable app for the dev container (scripts/assemble.sh).
/deploy
*.tsbuildinfo
# Ed25519 license test keys (secret seed). Never commit.
/dev/license-test-keys.txt
